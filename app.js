document.addEventListener('DOMContentLoaded', function() {
    // Get the current page URL
    const currentPage = window.location.pathname.split('/').pop();
  
    // Perform actions based on the current page
    switch (currentPage) {
      case 'dashboard.html':
        // Add specific logic for the dashboard page (if needed)
        break;
  
      case 'login.html':
        // Add specific logic for the login page (if needed)
        break;
  
      case 'submission.html':
        // Add specific logic for the submission form page (if needed)
        break;
  
      case 'contact.html':
        // Add specific logic for the contact information page (if needed)
        break;
  
      case 'logout.html':
        // Add specific logic for the logout page (if needed)
        break;
  
      default:
        // Default logic for other pages (if needed)
        break;
    }
  });
  
  // You can add more global functions or variables as needed
  